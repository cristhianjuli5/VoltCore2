<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    style="@style/Widget.VoltCore.Card"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/margin_medium"
    app:cardCornerRadius="@dimen/card_corner_radius_medium">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="@dimen/margin_medium"
        android:gravity="center_vertical">

        <FrameLayout
            android:layout_width="@dimen/thumbnail_size_medium"
            android:layout_height="@dimen/thumbnail_size_medium">
            <ImageView
                android:id="@+id/ivProduct"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@color/glass_surface"
                android:src="@drawable/ic_voltcore_logo"
                android:scaleType="centerCrop"
                android:contentDescription="@string/product_image_desc"/>
            <TextView
                android:id="@+id/tvProductEmoji"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center"
                android:textSize="@dimen/text_size_emoji_mid"
                android:visibility="gone"
                android:textColor="@color/volt_text_primary" />
        </FrameLayout>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:layout_marginStart="@dimen/margin_standard">

            <TextView
                android:id="@+id/tvProductName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/product_name_placeholder"
                android:textColor="@color/volt_text_primary"
                android:textSize="@dimen/text_size_body"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/tvProductPrice"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/price_placeholder"
                android:textColor="@color/volt_primary"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/tvProductStock"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/stock_hint"
                android:textColor="@color/volt_text_primary"
                android:textSize="@dimen/text_size_price"
                android:alpha="0.8"
                android:layout_marginTop="@dimen/margin_xtiny"/>

            <com.google.android.material.chip.Chip
                android:id="@+id/chipStatus"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/online_status"
                app:chipBackgroundColor="@color/glass_surface"
                android:textColor="@color/volt_primary"
                android:textSize="@dimen/text_size_caption"
                app:chipMinHeight="@dimen/margin_large"
                app:chipStrokeWidth="0dp"
                android:layout_marginTop="@dimen/margin_tiny"/>
        </LinearLayout>

        <ImageButton
            android:id="@+id/btnEdit"
            android:layout_width="@dimen/min_touch_target"
            android:layout_height="@dimen/min_touch_target"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/ic_menu_edit"
            app:tint="@color/volt_primary"
            android:contentDescription="@string/edit_icon_desc" />

        <ImageButton
            android:id="@+id/btnArchive"
            android:layout_width="@dimen/min_touch_target"
            android:layout_height="@dimen/min_touch_target"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/ic_menu_close_clear_cancel"
            app:tint="@color/volt_error"
            android:contentDescription="@string/delete_icon_desc" />

    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
